# Architecture Diagrams

## 🎯 Purpose

Visual representations of network concepts, API architectures, and system designs.

## 📊 Basic Network Architecture

### Client-Server Model

```
┌─────────────┐                    ┌─────────────┐
│   Client    │                    │   Server    │
│  (Browser)  │◄──────────────────►│  (Web App)  │
│             │   HTTP Request     │             │
│             │   HTTP Response    │             │
└─────────────┘                    └─────────────┘
```

### Three-Tier Architecture

```
┌──────────────────┐
│  Presentation    │  ← Web Browser/Mobile App
│     Layer        │
└────────┬─────────┘
         │
         │ HTTP/HTTPS
         │
┌────────▼─────────┐
│   Application    │  ← API Server/Business Logic
│     Layer        │
└────────┬─────────┘
         │
         │ SQL
         │
┌────────▼─────────┐
│      Data        │  ← Database
│     Layer        │
└──────────────────┘
```

### Microservices Architecture

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│ Client  │────►│   Gateway   │────►│   Auth      │
└─────────┘     │  (API GW)   │     │  Service    │
                └──────┬──────┘     └─────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
    ┌────▼───┐    ┌───▼────┐   ┌───▼────┐
    │  User  │    │ Order  │   │Product │
    │Service │    │Service │   │Service │
    └────┬───┘    └───┬────┘   └───┬────┘
         │            │            │
         └────────────┼────────────┘
                      │
                 ┌────▼─────┐
                 │ Database │
                 │  Layer   │
                 └──────────┘
```

## 🌐 Network Flow Diagrams

### HTTP Request Flow

```
1. DNS Resolution
┌──────┐      ┌─────┐
│Client│─────►│ DNS │
└──────┘      └──┬──┘
               │
               │ Returns IP: 93.184.216.34
               │
2. TCP Connection
┌──────┐      ┌────────┐
│Client│◄────►│ Server │
└──────┘      └────────┘
   SYN ───────────►
   ◄────────── SYN-ACK
   ACK ───────────►

3. HTTP Request/Response
┌──────┐      ┌────────┐
│Client│      │ Server │
└──────┘      └────────┘
   GET /page.html ──────►
   ◄────── 200 OK + HTML
```

### WebSocket Connection Flow

```
┌──────┐                    ┌────────┐
│Client│                    │ Server │
└───┬──┘                    └───┬────┘
    │                           │
    │ HTTP Upgrade Request      │
    ├──────────────────────────►│
    │                           │
    │ 101 Switching Protocols   │
    │◄──────────────────────────┤
    │                           │
    │  WebSocket Connection     │
    │◄─────────────────────────►│
    │     (Bidirectional)       │
    │                           │
    │ Message ──────────────────►│
    │◄────────────────── Message│
    │                           │
```

## 🔒 Authentication Flow Diagrams

### Session-Based Authentication

```
┌──────┐           ┌────────┐         ┌──────────┐
│Client│           │ Server │         │ Database │
└───┬──┘           └───┬────┘         └────┬─────┘
    │ 1. Login         │                   │
    │ ────────────────►│                   │
    │                  │ 2. Verify         │
    │                  │ ─────────────────►│
    │                  │◄──────────────────┤
    │                  │ 3. Create Session │
    │                  │ ─────────────────►│
    │ 4. Set Cookie    │                   │
    │◄─────────────────┤                   │
    │                  │                   │
    │ 5. Request       │                   │
    │  + Cookie        │                   │
    │ ────────────────►│ 6. Verify Session │
    │                  │ ─────────────────►│
    │ 7. Response      │◄──────────────────┤
    │◄─────────────────┤                   │
```

### JWT Authentication

```
┌──────┐           ┌────────┐
│Client│           │ Server │
└───┬──┘           └───┬────┘
    │ 1. Login         │
    │ ────────────────►│
    │                  │ 2. Verify credentials
    │                  │ 3. Generate JWT
    │ 4. Return JWT    │
    │◄─────────────────┤
    │                  │
    │ (Store JWT       │
    │  in localStorage)│
    │                  │
    │ 5. Request       │
    │  + Bearer Token  │
    │ ────────────────►│
    │                  │ 6. Verify JWT
    │                  │    (No DB lookup!)
    │ 7. Response      │
    │◄─────────────────┤
```

### OAuth 2.0 Flow

```
┌──────┐    ┌─────────┐    ┌──────────┐    ┌──────────┐
│Client│    │Your App │    │Auth Server│   │Resource  │
│      │    │         │    │ (Google)  │   │  Server  │
└───┬──┘    └────┬────┘    └─────┬─────┘   └────┬─────┘
    │            │               │               │
    │1. Login    │               │               │
    │───────────►│               │               │
    │            │2. Redirect    │               │
    │◄───────────┤──────────────►│               │
    │            │               │               │
    │3. User approves           │               │
    │──────────────────────────►│               │
    │            │4. Auth Code   │               │
    │            │◄──────────────┤               │
    │            │5. Exchange    │               │
    │            │  for token    │               │
    │            │──────────────►│               │
    │            │6. Access Token│               │
    │            │◄──────────────┤               │
    │            │               │               │
    │            │7. Request data│               │
    │            │  + token      │               │
    │            │───────────────┼──────────────►│
    │            │               │8. Return data │
    │            │◄──────────────┼───────────────┤
```

## 🔐 Security Architecture

### Defense in Depth

```
┌────────────────────────────────────────────┐
│            Perimeter Security              │
│  ┌──────────────────────────────────────┐  │
│  │        Network Security              │  │
│  │  ┌────────────────────────────────┐  │  │
│  │  │     Application Security       │  │  │
│  │  │  ┌──────────────────────────┐  │  │  │
│  │  │  │    Data Security         │  │  │  │
│  │  │  │  ┌────────────────────┐  │  │  │  │
│  │  │  │  │   Your Data        │  │  │  │  │
│  │  │  │  └────────────────────┘  │  │  │  │
│  │  │  └──────────────────────────┘  │  │  │
│  │  └────────────────────────────────┘  │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘

Layers:
1. Firewall, DDoS protection
2. SSL/TLS, VPN, Network segmentation
3. Authentication, Authorization, Input validation
4. Encryption at rest, Access controls
5. Your actual data
```

## 📡 API Design Patterns

### RESTful API Structure

```
Base URL: https://api.example.com/v1

Resource Hierarchy:
/users
  ├── /users                    (GET: list, POST: create)
  ├── /users/{id}              (GET: read, PUT: update, DELETE: delete)
  ├── /users/{id}/posts        (GET: user's posts)
  └── /users/{id}/followers    (GET: user's followers)

/posts
  ├── /posts                    (GET: list, POST: create)
  ├── /posts/{id}              (GET: read, PUT: update, DELETE: delete)
  └── /posts/{id}/comments     (GET: post's comments)

/comments
  └── /comments/{id}            (GET, PUT, DELETE)
```

### API Gateway Pattern

```
┌──────────┐
│  Client  │
└─────┬────┘
      │
      ▼
┌─────────────────┐
│   API Gateway   │
│   - Auth        │
│   - Rate limit  │
│   - Logging     │
└────────┬────────┘
         │
    ┌────┴────┬────────┬────────┐
    ▼         ▼        ▼        ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│User API│ │Post API│ │Pay API │ │Mail API│
└────────┘ └────────┘ └────────┘ └────────┘
```

## 🌍 CDN Architecture

```
User in USA             User in EU              User in Asia
     │                       │                       │
     ▼                       ▼                       ▼
┌─────────┐            ┌─────────┐            ┌─────────┐
│   CDN   │            │   CDN   │            │   CDN   │
│  USA    │            │   EU    │            │  Asia   │
└────┬────┘            └────┬────┘            └────┬────┘
     │                      │                      │
     └──────────────────────┼──────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │ Origin Server │
                    │  (Main API)   │
                    └───────────────┘

Benefits:
- Faster response times (closer to users)
- Reduced load on origin server
- Better availability
```

## 🔄 Load Balancing

```
                ┌──────────────┐
                │ Load Balancer│
                └──────┬───────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
    ┌────▼────┐   ┌───▼────┐   ┌───▼────┐
    │Server 1 │   │Server 2│   │Server 3│
    └─────────┘   └────────┘   └────────┘

Algorithms:
- Round Robin: Distribute evenly
- Least Connections: Send to server with fewest connections
- IP Hash: Same client → same server
```

## 📝 Data Flow Diagrams

### CRUD Operations Flow

```
CREATE:
Client ─────► POST /api/users ─────► Server ─────► DB
        JSON                              INSERT
       ◄───── 201 Created ◄───────────────────────┘

READ:
Client ─────► GET /api/users/123 ───► Server ─────► DB
       ◄───── 200 OK + JSON ◄─────────── SELECT ───┘

UPDATE:
Client ─────► PUT /api/users/123 ───► Server ─────► DB
        JSON                              UPDATE
       ◄───── 200 OK ◄────────────────────────────┘

DELETE:
Client ─────► DELETE /api/users/123 ► Server ─────► DB
       ◄───── 204 No Content ◄───────── DELETE ────┘
```

## 🎯 Tips for Creating Diagrams

1. **Keep it simple**: Don't overcomplicate
2. **Use consistent symbols**: Stick to conventions
3. **Label everything**: Clear names for components
4. **Show data flow**: Use arrows
5. **Include key details**: Protocols, methods, status codes
6. **Use tools**: draw.io, Lucidchart, or ASCII art

## 🔗 Resources

- [draw.io](https://draw.io/) - Free diagramming tool
- [Excalidraw](https://excalidraw.com/) - Sketch-style diagrams
- [PlantUML](https://plantuml.com/) - Text-to-diagram
- [Mermaid](https://mermaid-js.github.io/) - Markdown diagrams
- [C4 Model](https://c4model.com/) - Software architecture diagrams
