version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - ENVIRONMENT=docker-compose
    # volumes:
    #   - .:/app  # Uncomment for development with live reload
    restart: unless-stopped
